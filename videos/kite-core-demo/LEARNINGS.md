# Evidence used before generation

1. `prompt_library/tested_fold_patterns.json`: keep the same input facts through the result; show two actions with a visible second result; retain the output during the CTA. Applied to the Relay notes, draft and feedback.
2. Fold's recorded failure “paper morph without text distortion”: retire old text before a surface transform and add new text after it settles. Applied to the Preview transition. These are subjective production lessons, not audience-test findings.
3. `docs/CRUMB_SCORECARD.md`: stationary product + one camera direction worked in `crumb_pastry_kling_v1`; camera push was stronger than requested. Supports a small Kling candidate test for a simple paper still life. It does not establish a universally best model or a directly compatible text-to-video route.
4. `artifacts/learning/core.json` and `src/amarillo/shot_plan.py`: exact UI routes to HyperFrames. No compatible reviewed paper-object T2V endorsement exists; this is a bounded new test. Historical selected outputs remain available in core evidence, but unrelated human-shot successes are not used to justify this shot.

This run uses only pre-Phase-2 core lessons. No movement library, conditioned footage or newer human-realism findings inform it. New shot review must decide acceptance, regardless of the older scorecard.
